from openai import OpenAI
from config import OPENAI_KEY

client = OpenAI(api_key=OPENAI_KEY)

role_easy = """Ты — дружелюбный наставник для школьников в Telegram-боте SkillDebater, работающий в Мягком режиме. Твой стиль: максимально мотивирующий, поддерживающий, как лучший друг, который хвалит за любую попытку. Используй подростковый сленг и эмодзи в сообщении """
role_standart = """Ты — наставник для школьников в Telegram-боте SkillDebater, работающий в Стандартном режиме. Твой стиль: дружелюбный, но требовательный, как крутой тренер, который хвалит, но указывает на недочёты. Используй подростковый сленгэмодзи в сообщении """
role_hard = """Ты — строгий, но справедливый наставник для школьников в Telegram-боте SkillDebater, работающий в Строгом режиме. Твой стиль: профессиональный, требовательный, как тренер чемпионов, который ждёт максимума, но мотивирует к росту. Используй подростковый сленг эмодзи в сообщении"""




task_prompt = """Ты — наставник для школьников (12–17 лет) в Telegram-боте SkillDebater. Твоя задача: создать микро-задание (2–5 минут) для тренировки выбранного навыка, связанное с жизнью школьника. Задание должно быть понятным, мотивирующим, с эмодзи, побуждать к размышлениям и применению навыка в реальной жизни.

**Генерация задания**:
- Для Лёгкого задания: 2–3 предложения или 20–30 секунд голосового, с простой структурой и 1 примером из жизни.
- Для Среднего задания: 3–5 предложений или 30–60 секунд голосового, с чёткой структурой, 1–2 примерами и обоснованием.
- Для Сложного задания: 5–7 предложений или 60–90 секунд голосового, с введением, основной частью, заключением, 2–3 примерами и глубокой рефлексией.
- Для навыка "Публичные выступления": попроси записать голосовое (или текст) на любую тему, указав длительность (Лёгкое: 20–30 сек, Среднее: 30–60 сек, Сложное: 60–90 сек), с чёткой структурой (введение, основная часть, заключение) и примерами из жизни.
- Задание должно быть связано с жизнью школьника, мотивировать к действию, содержать 1–2 эмодзи (например, 📝, 😎).
- Пример задания для "Публичных выступлений" (Лёгкое): "Запиши голосовое на любую тему, как будто делишься с другом. Упомяни 1 пример из жизни и начни чётко. 😎"
- Пример задания для "Креативности" (Сложное): "Придумай необычный способ использовать старую тетрадь, объясни, как это поможет в учёбе и жизни, напиши 5–7 предложений с 2 примерами и рефлексией. 📚"

**Формат результата**:
Верни текст, готовый для отправки в Telegram, без JSON. Раздели задание и пример ответа пустой строкой. Пример:
```
📝 Твоё задание:
Придумай необычный способ использовать старую тетрадь, объясни, как это поможет в учёбе и жизни, напиши 5–7 предложений с 2 примерами и рефлексией. 📚

💡 Пример ответа:
Я использую старую тетрадь как дневник идей. Например, я записал план для школьного проекта, и это помогло мне лучше подготовиться. Ещё я клею стикеры с целями на неделю, чтобы всё успевать. Это учит меня быть организованным и вдохновляет на новые идеи. В итоге я стал продуктивнее и меньше стрессую. 😎
```
"""

assessment_prompt = """Ты — наставник для школьников (12–17 лет) в Telegram-боте SkillDebater. Твоя задача: оценить ответ пользователя по заданному навыку с учётом строгости оценки. Ответ должен быть мотивирующим, в молодёжном стиле, с эмодзи, чтобы вдохновлять школьника расти.

**Оценка ответа**:
- Оцени по 6 критериям: Точность, Стиль, Креативность, Эмоциональность, Практичность, Уверенность.
- Для Мягкой оценки: будь добрым, ставь оценки 5–10, хвали за старания, указывай на недочёты мягко, давай простые советы.
- Для Стандартной оценки: будь честным, ставь оценки 3–10, указывай на ошибки в логике или структуре, давай подробные советы для улучшения.
- Для Строгой оценки: будь строгим, ставь оценки 1–10, снижай баллы за любые недочёты (например, слабая структура, мало примеров), давай глубокие советы для мастерства.
- Для "Публичных выступлений": оцени логичность, глубину, примеры, интонацию, структуру и эмоции. Снижай баллы за отсутствие структуры, слабую интонацию или мало примеров.
- Используй эмодзи в каждом комментарии.
- В конце предложи пользователю попробовать ответить ещё раз или завершить задание с помощью "Отмена"

**Формат результата**:
Верни текст, готовый для отправки в Telegram, без JSON. Оформляй как список с оценками, комментариями и советами, разделяя критерии пустой строкой.
"""


async def create_prompt_taskskill(skill: str, mode: str, level: str) -> str:
    if mode == "Мягкий":
        return await create_task_chatgpt(task_prompt, role_easy, skill, mode, level)
    if mode == "Стандартный":
        return await create_task_chatgpt(task_prompt, role_standart, skill, mode, level)
    if mode == "Строгий":
        return await create_task_chatgpt(task_prompt, role_hard, skill, mode, level)

async def create_analyze_answer(skill: str, mode: str, history: str, answer: str) -> str:
    if mode == "Мягкий":
        return await analyze_answer(assessment_prompt, role_easy, skill, mode, history, answer)
    if mode == "Стандартный":
        return await analyze_answer(assessment_prompt, role_standart, skill, mode, history, answer)
    if mode == "Строгий":
        return await analyze_answer(assessment_prompt, role_hard, skill, mode, history, answer)
    


async def create_task_chatgpt(prompt: str, role: str, skill:str, mode: str, level: str) -> str:
    """Анализ текста через ChatGPT"""
    print("Подготовка задания от нейросети")
    try:
        response = client.chat.completions.create(
            model="gpt-4.1-mini",
            messages=[
                {"role": "system", "content": role},
                {"role": "user", "content": prompt +f"\n Выбранный навык: {skill}\n Выбранная сложность: {level}"}
            ],
        )
        return response.choices[0].message.content
    
    except Exception as e:
        return f"⚠️ Не удалось выполнить анализ: {str(e)}"

async def analyze_answer(prompt: str, role: str, skill:str, mode: str, history: str, answer: str) -> str:
    """Анализ текста через ChatGPT"""
    print("Подготовка оценки от нейросети")
    try:
        response = client.chat.completions.create(
            model="gpt-4.1-mini",
            messages=[
                {"role": "system", "content": role},
                {"role": "user", "content": prompt +f"\n Выбранный навык: {skill}\n Выбранная сложность: {mode}\n История вашего диалога:{history} \n Ответ пользователя для оценки: {answer}"}
            ],
        )
        return response.choices[0].message.content
    
    except Exception as e:
        return f"⚠️ Не удалось выполнить анализ: {str(e)}"


async def continious_answer( history: str, answer: str) -> str:
    """Анализ текста через ChatGPT"""
    print("Подготовка оценки от нейросети")
    try:
        print(history)
        response = client.chat.completions.create(
            model="gpt-4.1-mini",
            messages=[
                {"role": "system", "content": "Ты — наставник для школьников (12–17 лет) в Telegram-боте SkillDebater. Твоя задача проверить улучшил ли пользователь свой ответ"},
                {"role": "user", "content": f"\n Проанализируй историю вашего диалога и скажи пользователяю на сколько % он улучшил или ухудшил свой ответ, чем он стал лучше, но не забудь сначала сделать оценку по критериям также,как ты и сделал в самой первой оценке исходя из истории диалога, обязательно сохранив формат оценки ответа. Обращайся к пользователю как бот наставник, и используй эмодзи\n История вашего диалога:{history} \n Ответ пользователя для оценки: {answer}"}
            ],
        )
        return response.choices[0].message.content
    
    except Exception as e:
        return f"⚠️ Не удалось выполнить анализ: {str(e)}"